import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export default function Articles_list() {
  return (
    <div>
      <h1>Página Principal</h1>
    </div>
  )
}
